# Runtime (pinned upper bounds for reproducibility of published results)
openai>=1.40.0,<2.0
httpx>=0.27.0,<1.0
python-dotenv>=1.0.1,<2.0
pydantic>=2.7.0,<3.0
typer>=0.12.3,<1.0
rich>=13.7.1,<14.0
streamlit>=1.36.0,<2.0
tenacity>=8.3.0,<10.0
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
numpy<=2.2

# Multi-modal structural layer (UniProt domain plot + AlphaFold 3-D viewer)
matplotlib>=3.8.0,<4.0
py3Dmol>=2.1.0,<3.0

# Multi-modal specialist models (optional — install only if you want the
# corresponding modality. On AMD AI Developer Cloud `torch` should be the
# pre-installed ROCm wheel; do NOT pip-install a CUDA torch over it.)
#
# ESM-2 (protein language model) and Whisper (speech) need torch+transformers:
transformers>=4.41.0,<5.0
Pillow>=10.0.0,<12.0
# Optional BiomedCLIP backend (preferred for histology/radiology):
# open_clip_torch>=2.24.0
# Optional Whisper audio decoding (uncomment if your audio inputs need it):
# soundfile>=0.12
# librosa>=0.10

# Dev / research-mode (install with: pip install pytest pytest-cov ruff)
# pytest>=8.0
# pytest-cov>=5.0
# ruff>=0.5
